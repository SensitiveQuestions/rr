language: r
cache: packages

env:
 global:
   - CRAN: http://cran.rstudio.com

r_packages:
  - testthat
  - nlme
  - lme4
  - arm
  - coda
  - MASS

warnings_are_errors: true

script: 
  - "Rscript -e 'library(devtools); test()'"
  - "cd .."
  - "R CMD build rr"
  - "R CMD CHECK rr_1.3.tar.gz"

notifications:
  email:
    on_success: change
    on_failure: change